import { Banner } from "../components/StateView";

export function QualityPage() {
  return (
    <main className="container">
      <Banner kind="muted">La página de calidad de datos (última corrida por país, cobertura, huecos) se construye en el siguiente paso de la Fase 4.</Banner>
    </main>
  );
}
